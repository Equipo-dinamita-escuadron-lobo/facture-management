version: "3.9"

services:
  db_thirds:
    image: mariadb:11.2.3
    restart: always
    environment:
      MARIADB_USER: springuser
      MARIADB_PASSWORD: ThePassword
      MARIADB_DATABASE: db_factures
      MARIADB_ROOT_PASSWORD: ThePassword
    #volumes:
    #  - ./data:/var/lib/mysql
    ports:
      - "3307:3306"
    container_name: db_factures
    labels:
      - "traefik.enable=false"

    networks:
      - softcont-network

  thirds_management:
    build: .
    ports:
      - "8081:8080"
    environment:
      - DB_USERNAME=springuser
      - DB_PASSWORD=ThePassword
      - DB_URL=jdbc:mariadb://db_factures:3306/db_factures
      - SECURITY_USER=root
      - SECURITY_PASSWORD=
    depends_on:
      - db_factures
    container_name: factures_management
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.factures_management.rule=PathPrefix(`/api/factures`)"
      - "traefik.http.services.factures_management.loadbalancer.server.port=8080"
    networks:
      - softcont-network

networks:
  softcont-network:
    external: true